{
  "name": "mysour",
  "version": "1.0.0",
  "description": "AI Assistant with image processing capabilities",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "if not exist dist mkdir dist && xcopy /E /I /Y static dist\\static && xcopy /E /I /Y templates dist\\templates && copy .env dist && copy server.js dist && copy package.json dist && copy index.html dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "openai": "^4.20.1",
    "sharp": "^0.32.6",
    "markdown-it": "^13.0.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 